// Inicializar Turn.js
$(document).ready(function() {
    $('.flipbook-container').turn({
        width: 800, // Ancho del flipbook
        height: 600, // Altura del flipbook
        autoCenter: true, // Centrar automáticamente
        duration: 1000, // Duración del giro
    });
});
